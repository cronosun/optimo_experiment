# This is required for jackson. Jackson needs to know the constructor properties (list of all fields) to be able
# to de-serialize a json. With this configutration we now can de-serialize immutable (all fields are final;
# no setters) lombok data classes.
# See https://stackoverflow.com/questions/49999492/immutable-lombok-annotated-class-with-jackson/50030088
lombok.anyConstructor.addConstructorProperties=true
